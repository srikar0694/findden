VITE_API_BASE_URL=http://localhost:5000/api
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
# Google Identity Services OAuth Client ID (for "Sign in with Google" — CR §0.1)
# Create at https://console.cloud.google.com/apis/credentials and add your
# dev origin (e.g. http://localhost:5173) to "Authorized JavaScript origins".
# If left blank the app falls back to a simulated Google profile (dev-only).
VITE_GOOGLE_CLIENT_ID=
